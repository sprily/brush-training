# Default framework settings

datahopper {

  request-interval = "500ms"

  generator-meter = {
    host = "192.168.1.200"
    port = 502
    unit = 5
  }

  grid-meter = {
    host = "192.168.1.200"
    port = 502
    unit = 6
  }

  mqtt = {
    url = "ssl://mqtt.sprily.co.uk"
    port = 8883
    username = "test"
    password = "fp1UzGYMaEyXFMNQ6tCrnaoB"

    root = "test-dev"
  }

  ## The place where the device configuration is stored
  device-config-file = ${user.home}/.brush-training-facility/devices

}

##############################################################################
# Logging configuration
##############################################################################

# Moved, see the logger.xml resource
